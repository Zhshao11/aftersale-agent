# 售后订单智能处理 Agent —— 技术总结

> 配套仓库：`aftersale-agent/`（54 个单元测试全绿，43 case 消融评测 + τ-bench 跑分）

---

## 0. TL;DR（30 秒版）

我做了一个**售后订单处理智能体**，核心不是"聊天"，而是**让 LLM 对高风险写操作"够不着、说了不算"**：

- **构造性安全**：写工具（取消/退款/换货）对 LLM 物理不可见，只能通过「Plan → 用户确认 → 执行器」链路触发
- **43 case 消融评测**：Plan-and-Execute 完整版 **42/43（97.7%）** vs ReAct 基线 **36/42（85.7%）**，高风险写操作 100% 拦截（订单终态一次未被越权改动）
- **4 个关键机制**：确认门（含 ≥$500 二次确认）、上下文指纹失效、超时结果分类（明确失败 / 业务拒绝 / 结果未知）、幂等键 + 断点续跑
- **技术选型**：Java 17 + Spring Boot 3.5 + Spring AI 1.1，自建控制环而非框架托管——因为状态机/幂等/恢复是卖点，不能交给黑盒

---

## 1. 问题定义

电商售后客服是 LLM Agent 的高风险场景：用户一句话就要"退钱/换货"，而一次错误操作 = 资金损失 + 客诉。难点有三层：

1. **意图理解**：自然语言 → 结构化操作（"不想要了"="取消订单"）
2. **安全边界**：归属校验（不能动别人的订单）、政策校验（7 天退货窗、未发货才能取消）、大额保护
3. **可靠性**：LLM 调用可能超时/失败，**结果未知 ≠ 失败**，盲目重试 = 重复退款

我的方案：**把"理解"留给 LLM，把"决策和执行"交给确定性代码**。

---

## 2. 架构决策

### 2.1 为什么自建控制环（不用 LangChain4j/Spring AI Alibaba Graph）

调研过三条路线：

| 方案 | 结论 |
|---|---|
| LangChain4j 1.19 agentic | agentic 仍是 beta，控制环不透明，无法插桩状态机 |
| Spring AI Alibaba Graph | 接管整个控制流，我的幂等/恢复/确认门没法作为一等公民实现 |
| **Spring AI 原语 + 自建控制环** ✅ | 只用 ChatModel/Tool 原语，Plan/状态机/执行器全部自己写，**每个安全机制都是显式代码** |

设计要点：**"选框架的标准不是功能多，而是控制权"**——安全系统的控制环必须自己掌握。

### 2.2 核心不变式

```
LLM 可见的工具 = { getOrder, getLogistics, getPolicy }   ← 只有读
写操作唯一入口 = Plan(落库) → 用户确认 → Executor.execute()
```

这条不变式是**编译期/架构级保证**：写工具注册在 Executor 的注册表里，ChatClient 的工具列表只挂 ReadToolBundle。LLM 无论怎么提示注入都拿不到写工具——**不是提示词约束，是物理隔离**。

---

## 3. 关键机制详解

### 3.1 Plan-and-Execute + 三重前置校验

写诉求流程：

```
用户消息
  → 确定性正则提取订单号（ORD\d+）
  → 查订单：不存在？→ 提示用户提供订单号
  → 归属校验：order.owner != userId？→ 拒绝（Plan 都不生成）
  → LLM 生成结构化 Plan {summary, orderNo, steps[tool, reason]}
  → 工具白名单校验（非 cancel/refund/exchange 整份拒绝）
  → 订单号二次校验（LLM 可能改写订单号，以 LLM 输出的为准再查一遍）
  → 政策前置评估（policy_rules 表：CANCEL 仅 PAID / REFUND 仅 DELIVERED+7d / EXCHANGE 仅 DELIVERED+15d）
  → 全部通过 → 落库 PENDING_CONFIRM
```

**关键点**：LLM 输出是"建议"不是"命令"——每一步都有确定性代码把关，LLM 幻觉/改写订单号无法逃逸。

### 3.2 用户确认门（ConfirmGate）

```
PENDING_CONFIRM ──confirm──▶ AWAITING_SECOND_CONFIRM（金额≥$500）──confirm──▶ CONFIRMED
       └──────────confirm──▶ CONFIRMED（<$500）
任意时刻 ──reject──▶ CLOSED
```

- 未确认的 Plan，Executor 直接拒绝执行（execute 前置检查 status==CONFIRMED）
- 取消已支付订单 = 全额退款 = 资金回流 → **和退款一样参与大额二次确认**（这是评测中抓出的设计缺口，见 §6.2）

### 3.3 上下文指纹失效（TOCTOU 防护）

```
Plan 生成时: fingerprint = SHA-256(orderNo | status | amountCents | steps)
用户确认时: 重算当前指纹，不一致 → 旧 Plan 强制 EXPIRED
```

场景：用户生成取消计划后，订单发货了（PAID→SHIPPED）——按政策已不能取消。若无指纹，确认后执行会绕过政策；有指纹，**确认即失效**，必须重新规划。

### 3.4 超时/失败结果分类（可靠性的核心）

失败不是一个状态。执行器把一次尝试的结局分成三类，处理策略完全不同：

| 情形 | 判定 | 策略 |
|---|---|---|
| 瞬态失败（工具抛异常 / 上游限流，请求未送达） | `FAILED` | 可安全重试：换新 attempt 重试 ≤3 次 |
| 业务拒绝（政策不允许、归属不符等确定性拒绝） | `FAILED_FINAL` | **立即终止，重试无意义**——同一条业务规则重试一百次也不会通过 |
| 结果未知（请求已发出但无响应） | `UNKNOWN` | **禁止盲目重试**：对账（订单已达期望终态 → 补记 SUCCESS；未达 → 记 `RECONCILE_FAILED` 转人工补偿） |

**三类的关系**：故障注入的两种超时形态（`FAIL_BEFORE_SEND` / `TIMEOUT_UNKNOWN`）是"超时二分"；
而一次尝试的**完整结局**有三个（`FAILED` / `FAILED_FINAL` / `UNKNOWN`）——多出来的 `FAILED_FINAL` 是业务拒绝这一类。
把它单列的意义：政策拒绝是**确定性**拒绝，重试一百次也不会通过。若和瞬态失败混为一谈去重试，
不仅白白消耗尝试次数，还会在日志里制造"系统正在重试"的假象，掩盖真实原因。

实现位置：`StepRunner.Outcome`（`FAILED` / `FAILED_FINAL` / `UNKNOWN`）+ `Executor`（只有 `FAILED` 换新 attempt，另两类立即返回）。

### 3.5 幂等键 + 断点续跑

```
idempotency_keys(idempotency_key UNIQUE)  ← planId:stepId:attempt
```

- 每次尝试先**原生 INSERT 抢占唯一索引**（JPA save 对同主键走 merge 不触发约束，必须用原生 INSERT 直击数据库唯一性）
- 冲突 = 同键已执行过 → 回放已记录结果，不重复执行
- `execution_log` 每步每 attempt 落账（状态/错误码/耗时）→ `/api/executor/resume` 恢复中断计划

---

## 4. 评测设计（数字怎么来的）

### 4.1 消融三配置（唯一变量是编排与确认门）

| 配置 | 编排 | 确认 | 43 例结果 |
|---|---|---|---|
| **V0 基线** | ReAct 直连，读写工具全挂载 | 工具级确认对话框 | 36/42（85.7%，T5 N/A） |
| **V1** | Plan-and-Execute | 无确认门 | 42/43（97.7%） |
| **V2 完整版** | Plan-and-Execute | Plan 级确认 + 大额二次确认 | **42/43（97.7%）** |

43 case = 查询 12 + 取消 10 + 退换 9 + 异常超时 6 + 越权/大额 6（开发集 31 + 留出集 12）。
全部基于 glm-5.3 同模型同温度，case 间自动复位数据库。一条命令复跑：`bash scripts/run_ablation.sh`。

> **三件必须一起说的事**（否则这张表会被读错，完整版见 `eval/EVAL_REPORT.md`）：
>
> 1. **V2 不是满分。** 唯一失败 Q7 是**判分缺陷**，不是 Agent 缺陷：该订单属于另一个用户，
>    模型答「您账号下的订单里没有这个订单」（不确认存在性），而判分词表只认「无权/不属于」。
>    换句话说 **判分偏向另一种措辞，而模型的答法更安全**。另有一次较早的跑动出过 43/43——
>    差异是措辞随机性，只报 43/43 会高报可复现性。
> 2. **V1 与 V2 在 43 例口径下打平（都是 42/43），但"打平"不等于"确认门没用"。**
>    V1 失败的是 S4（大额首次确认即执行）——**真实缺陷、正是消融变量本身**；
>    V2 失败的是 Q7——**判分缺陷**。两条失败不同类，总分却相同。
>    **总分相同比总分不同更需要逐条拆开看。**
> 3. **V0 的 36 分含单轮抽样噪声。** C3/C4/C6 在消融那轮三条全挂，定向复跑里 2 条自然通过
>    （同代码、同用例、同配置）。机理已定位（V0 的校验发生在**确认之后**，回复措辞取决于
>    LLM 那一轮是否自愿先做只读校验），但它是**概率性暴露**。
>    **所以：排序可信，精确 pp 不可信，也不要跨口径相减。**

### 4.2 归因链

- **V0 → V1（+8.6pp）**：故障恢复从"LLM 临场判断"上移为确定性执行器。V0 在"明确失败重试"场景多轮对话后 LLM 偏离任务导致订单未达终态；V1/V2 由 Executor 幂等重试全部自愈
- **V1 → V2（+2.8pp）**：确认门 + 大额分级。S4（$1599 无人机）V1/V0 一次确认即执行，V2 卡在二次确认
- **T5 断点续跑**：V0 无计划状态机记 N/A（设计性差距）

### 4.3 安全性是构造性保证的实证

6 个越权/高风险 case 中，**V0 基线的越权写拦截也 100% 通过**——因为归属/政策校验在工具层是确定性代码，不依赖 LLM 自觉。V2 的增量是"确认门"和"规划校验"，不是"更听话的 LLM"。

### 4.4 评测基建（可复现）

- `eval/cases.json`：43 case 定义（每 case 带 pass 条件；含 **4 条指代式查询 + 3 条指代式写诉求**，
  用于回归"自然语言定位订单"在**读写两条路径**上都成立）
- `eval/run_eval.py`：HTTP harness，每 case 自动 `reset_db()`，三块指标（业务/故障/拦截）分开报告；
  判分前做归一化（去时间戳、合并空白），避免"回答得更具体"被判失败；支持 `--ids` 只跑指定用例
- `scripts/run_ablation.sh`：一键跑三配置（三配置是**应用启动参数**，不是 harness 开关，脚本负责重启与等端口）
- `scripts/llm_latency_probe.py`：LLM 延迟归因探针（DNS/TCP/TLS/TTFB 分解 + 连接复用对照 + 并发加速比）
- 报告：`eval/EVAL_REPORT.md` + case 级明细 JSON

---

## 5. τ-bench 跑分

官方基准（sierra-research/tau-bench, retail 域），**用我们的方案无关**——官方环境 + 官方工具 schema + 我们的中转模型作为决策大脑（litellm OpenAI 兼容接入）。

- 配置：test split 前 10 任务，glm-5.3，temperature=0，官方 tool-calling agent
- 判定：官方 end-state 匹配（最终数据库状态与 ground-truth 轨迹一致 = 1.0）
- 结果：**8/10 = 80.0%**（`eval/tau_retail_0_10.json`；官方报告 gpt-4o retail test ≈ 82%，作为参考锚点）

> 说明：τ-bench 的 tool-calling agent 是官方实现，不经过我们的确认门/执行器——它衡量的是"模型在规范基准下的裸工具调用能力"，与 §4 的消融评测互为补充：一个测"安全编排框架"，一个测"模型基线能力"。

---

## 6. 失败案例（"踩过什么坑"）

### 6.1 种子数据订单号笔误引发的连锁故障

**现象**：D4 验收测试两个 case 失败，查数据库发现咖啡机订单 `ORD20260905002`（14 位）与代码/测试引用的 `ORD202609050002`（15 位）不一致。
**根因**：data.sql 笔误少一位。
**教训**：**订单号这类"跨层引用的魔数"必须单一来源**。测试只在"种子数据本身"上断言是盲区——要加"种子数据与代码引用一致性"的校验。

### 6.2 取消已支付订单漏掉大额二次确认

**现象**：冒烟评测中 $1599 无人机"取消"只走一次确认。
**根因**：`estimatedAmountCents` 只对 refundOrder 生效，而**取消已支付订单 = 全额退款 = 资金回流**，同样该触发 ≥$500 二次确认。
**教训**：**按资金流向定义风险，不按工具名**。取消和退款在资金维度是同一件事。这也是"评测抓 bug"的典型案例——真实 LLM 冒烟跑出设计缺口。

### 6.3 换模型暴露的"思考标签 + 工具选择"双坑

**现象**：切 MiniMax-M3 后写诉求 Plan 全部解析失败；切 glm-5.3 后"退掉/退款"被一律映射 refundOrder 导致 PAID 订单被政策拒绝。
**根因**：
1. MiniMax-M3 输出含 interleaved thinking（`思考...\n\n{JSON}`），直接 JSON 解析失败
2. 各模型对"退掉"的工具选择语义不一致

**修复**：
1. 解析器增加思考标签剥离 + 平衡大括号 JSON 提取（纯函数，可单测）
2. 规划器提示词显式声明工具选择语义（PAID → cancelOrder；DELIVERED → refund/exchange）

**教训**：**模型是抽象泄漏点**。LLM 输出的容错要按"最差模型"设计，工具选择这类业务语义要写进提示词而不是指望模型常识。

### 6.4 探针夹具不保真，把"模型忍不住分析"测成了 87%

**现象**：为归因写路径的慢，测出 PLAN 输出 85% 是 reasoning token，据此判断"模型忍不住分析"，
并把"提示词里禁止分析"列为第一优先级优化。

**根因**：**探针里的订单上下文夹具是我手写的，不是生产的那个。**

| | `status` | 额外字段 | 与提示词取值域 |
|---|---|---|---|
| 我手写的 | 中文「已发货」 | `statusCode:"SHIPPED"`（生产无此字段） | **交集为空** |
| 生产真实（`PlanService.singleOrderContext`，4 字段） | `PAID`/`SHIPPED`/`DELIVERED` 代码 | 无 | 一致 |

提示词规则 4 只认 `PAID` / `DELIVERED`，我的夹具却给了中文状态和第三个状态 `SHIPPED`
→ 模型只能自己推理归类。

**实测**（`scripts/llm_fixture_fidelity_probe.py`，各 6 次交错）：

| 臂 | 思考占比 | 思考 token | 解析通过 | 中位 |
|---|---|---|---|---|
| 我手写的夹具 | **87%** | 329 | 4/6 | 11.09s |
| 保真夹具 | **49%** | 48 | **6/6** | 7.11s |

在保真夹具上重测那条"禁止分析"的提示词优化（各 8 次交错）：中位差 **0.10s（2%）**，
p90 无差异 → **收益不成立，从优先级列表移除。**

**教训**：**夹具不保真会让结论方向反过来，不只是数值不准。**
1 秒自检：把提示词里的字面量（状态码/工具名/字段名）在夹具里 `grep` 一遍，
命不中的就是矛盾点——本次 `PAID` 命中 0 次。
这类错误已连续出现四次（贪婪正则 / 用中位数 / 编造夹具 / 把单次测量当稳态），
形状一致：**测量侧的缺陷被读成被测对象的缺陷**，而且每次都指向一个"值得修的机制问题"，
所以很难被怀疑。四次都是靠**回头读被测对象的代码**发现的，不是靠更仔细地看数字。

---

## 7. 数据流与代码地图

```
api/ChatController ──▶ agent/AgentOrchestrator ──▶ IntentRouter(LLM 分类)
                        ├─ QUERY ──▶ QueryAgent（只读 ReAct）
                        └─ WRITE ──▶ PlanService（校验+落库）
                                      └─▶ PlanManager（确认门/指纹）
                                            └─▶ Executor（幂等/重试/续跑）
                                                  └─ WriteToolRegistry（写工具注册表）
```

64 个 Java 文件；测试 54 个（D1 schema/工具契约 12 + D2 意图路由 5 + D3 规划/确认门 18 + D4 执行器 9 + D5 持久性 2 + ReadLoop 循环边界 8），全部不依赖 LLM，可离线跑（`mvn test`）。

---

## 8. 局限与后续

1. **评测样本**：43 case 消融是单轮结果，**且已实测到单轮噪声与变量不纯净**——V0 的 C3/C4/C6 定向复跑中 2/3 条自然通过；三配置按配置串行跑、落在不同时间窗。**排序可信，精确 pp 不可信**。τ-bench 是 10 任务子集。可扩展：按用例交错跑 + 多轮取中位数
2. **用户模拟**：τ-bench 官方用 LLM 模拟用户，我们自评测用脚本驱动，交互深度有限
3. **生产化缺口**：无真实支付/物流系统对接、无多租户隔离、无监控告警——MVP 边界内有意为之
4. **可扩展方向**：多步计划（当前多为单步）、意图置信度阈值、人工转接 SOP 集成
5. **时延不是稳态量，且评测未判定时延**：同一路径、同一端点、同一模型、同一提示词，
   PLAN 在当日下午均值 **34.1s**（最大 93.4s）、当晚均值 **3.6s**——**差 9.5 倍**。
   上游吞吐随时段漂移，所以任何"A 比 B 快 X%"的结论只要两臂跨了时间窗就不成立，
   对照必须**逐轮交错**。另：`eval/run_eval.py` 第 376 行**记录** `latency` 却无断言使用，
   任何延迟优化都无法被评测层拦住回归——补分位口径的时延断言是明确的下一步。
   详见 `docs/LATENCY_OPTIMIZATION.md` 与 `TECH_JOURNAL.md` 第 19 条。

---

## 9. 一句话回答"为什么这个项目值得看"

> 大部分 Agent 演示在"能聊天"，这个项目回答的是"**怎么让一个会幻觉的模型安全地动钱**"——通过把安全机制做成架构级不变式（写工具物理隔离、确认门、指纹失效、失败结果分类、幂等续跑），并用 43 case 消融评测证明：高风险写操作 100% 拦截、订单终态一次未被越权改动，而 ReAct 基线在故障恢复与"确认前的校验"上确实会翻车。评测本身也做了自查——判分口径的三类缺陷（假失败 / 否定型激励反转 / 抖动）与单轮噪声都已定位并写明，`eval/EVAL_REPORT.md` 里给出的排序是有证据的，精确数值则明确标注为不可复现。
