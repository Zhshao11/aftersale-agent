# 售后订单智能 Agent — 使用说明（体验版）

## 一、如何访问

**地址**：http://localhost:8081

应用已启动在 8081 端口，直接用浏览器打开即可，无需登录。

> 其他端口时把链接里的 8081 换成实际端口即可。

---

## 二、页面布局

| 区域 | 说明 |
|---|---|
| 左侧聊天区 | 输入你要说的话，Agent 会分类处理（查询 / 写操作） |
| 意图标签 | 每条回复上方会显示识别出的意图（QUERY=查询 / WRITE=写操作） |
| **Plan 卡片** | 写操作时会出现的橙色卡片，展示：订单号 / 摘要 / 金额 / 是否需二次确认 / 状态徽章 |
| 卡片按钮 | **确认 / 拒绝 / 执行** 三个按钮（只有确认后才亮起"执行"） |
| 右上角用户切换 | U001（演示主用户）↔ U002（越权演示用户） |
| 消息区下方示例 | 7 个示例话术，一键填入聊天框 |

---

## 三、可以体验的核心玩法（按推荐顺序）

### ① 查询类（安全操作，直接回答）
试试：
- `查一下 ORD20260901001 的物流`
- `订单 ORD202609050002 到哪里了`
- `售后退货政策是什么`

**看点**：Agent 识别为 QUERY，直接调用只读工具回答，不经过确认流程。

### ② 写操作 → Plan 卡片 → 确认 → 执行（核心演示流程）
试试：
- `帮我取消订单 ORD20260901001`
- `帮我取消订单 ORD202609060006`（这单金额 $549.99，注意差异）

**流程**：输入 → 生成 Plan 卡片（PENDING_CONFIRM）→ 点【确认】→ 再点【执行】→ 完成。

**看点**：
- 未确认前"执行"按钮不可用——写操作必须先过确认门
- 金额 ≥ $500 的订单会要求**二次确认**（状态先变 AWAITING_SECOND_CONFIRM，需点两次确认）
- 执行后刷新页面数据（右上角刷新按钮），订单状态会从 PAID 变成 CANCELLED

### ③ 越权拦截（安全亮点）
- 切到 **U002**，输入：`帮我取消订单 ORD20260901001`（这单属于 U001）
- Agent 会直接拒绝：订单不属于当前用户

**看点**：即使你告诉它订单号，归属校验也会拦下——这是代码级校验，不是靠 LLM 自觉。

### ④ 退货 / 换货（政策约束）
试试（注意先切回 U001）：
- `ORD202609080005 这个商品我要退货`（DELIVERED 单，允许退款）
- `ORD20260901001 我要退货`（PAID 单，政策上只能取消不能退款）

**看点**：同一种说法，Agent 会根据订单状态走不同工具——同样的话术，不同订单得到不同 Plan。

---

## 四、故障注入（可选，看执行可靠性）

后端开启了评测用故障注入开关时：
- 在取消订单的确认后执行过程中，可注入超时/失败
- **明确失败**：自动重试（≤3 次）后成功
- **结果未知**：不盲目重试，订单进入对账/待补偿状态

> 体验版默认不开启注入，如需演示可在启动参数里加 `--aftersale.inject-timeout-after=xxx`。

---

## 五、多轮对话小提示

- 第一句没给订单号时，Agent 会**反问你要订单号**——补上订单号再来一句即可（会话会记住上下文）
- 想换话题直接说，不用开新会话

---

## 六、演示完数据复位（重要）

演示会真的修改数据库（取消的订单会变 CANCELLED）。再次演示前复位：

```bash
docker exec aftersale-mysql mysql -uroot -proot123 aftersale \
  -e "UPDATE orders SET status='PAID' WHERE status='CANCELLED' AND order_no='ORD20260901001';
      TRUNCATE TABLE plans;
      DELETE FROM conversations;"
```

（或者直接重启镜像重建 schema+data 也是干净状态）

---

## 七、演示用户与订单速查（演示讲解用）

| 用户 | 说明 |
|---|---|
| U001 | 主演示用户，12 条订单 |
| U002 | 仅作越权演示目标（别用它买/退，订单少） |

U001 高价值演示订单：

| 订单号 | 状态 | 金额 | 演示点 |
|---|---|---|---|
| ORD202609150011 | PAID | $1,599.00 | **高额二次确认**（>$500） |
| ORD202609060006 | PAID | $549.99 | 二次确认（>$500） |
| ORD20260901001 | PAID | $89.99 | 普通取消（一次确认） |
| ORD202609080005 | DELIVERED | $129.00 | 退款演示 |
| ORD202609050002 | SHIPPED | $459.50 | 物流查询 |
| ORD20260910003 | DELIVERED | $699.00 | 退款/换货（>$500 也要二次确认） |